# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Take That Break' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Sparkle'
  # Pods for Take That Break

  target 'Take That BreakTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Take That BreakUITests' do
    # Pods for testing
  end

end
